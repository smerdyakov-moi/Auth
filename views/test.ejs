<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
  <div class="flex items-center justify-center min-h-screen px-4">
    <div class="w-full max-w-md bg-zinc-800 rounded-2xl shadow-xl p-8">
      <h3 class="text-3xl font-semibold mb-6 text-center text-white">Upload Image</h3>
      <form action="/uploadFile" method="post" class="space-y-4" enctype ="multipart/form-data">
        <input type = "file" name ="image">
        <input class="w-full px-4 py-2 rounded-lg bg-green-500 hover:bg-green-600 text-white font-semibold cursor-pointer transition" type="submit" value="Upload File">
      </form>
      <form action ="/removeProfilePic" method ="post" class="space-y-4">
        <input class="w-full px-4 py-2 rounded-lg bg-red-500 hover:bg-red-600 text-white font-semibold cursor-pointer transition" type="submit" value =" Remove profile pic">
      </form>
    </div>
  </div>
</body>
</html>
