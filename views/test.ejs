<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-green-950 text-green-100">

  <div class="flex items-center justify-center min-h-screen px-4">
    <div class="w-full max-w-md bg-green-900 rounded-2xl shadow-xl p-8">
      <h3 class="text-3xl font-semibold mb-6 text-center text-green-300">Upload Image</h3>
      
      <!-- Upload Form -->
      <form action="/uploadFile" method="post" class="space-y-4" enctype="multipart/form-data">
        <input type="file" name="image" class="w-full px-4 py-2 rounded-lg bg-green-800 border border-green-700 text-green-100 focus:outline-none focus:ring-2 focus:ring-green-500" />
        <input class="w-full px-4 py-2 rounded-lg bg-green-600 hover:bg-green-700 text-white font-semibold cursor-pointer transition" type="submit" value="Upload File">
      </form>
      
      <!-- Remove Profile Pic Form -->
      <form action="/removeProfilePic" method="post" class="space-y-4 mt-6">
        <input class="w-full px-4 py-2 rounded-lg bg-red-600 hover:bg-red-700 text-white font-semibold cursor-pointer transition" type="submit" value="Remove Profile Pic">
      </form>
    </div>
  </div>

</body>
</html>
