
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>User Post Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col">
  
  <!-- Top Bar -->
  <header class="w-full flex justify-end p-4 bg-zinc-800 shadow">
    <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white text-sm px-4 py-2 rounded-md transition">Logout</a>
  </header>
  <header class="w-full flex justify-end p-4 bg-zinc-800 shadow">
    <a href="/editProfile" class="bg-red-500 hover:bg-red-600 text-white text-sm px-4 py-2 rounded-md transition">Edit Profile</a>
  </header>
  <header class="w-full flex justify-end p-4 bg-zinc-800 shadow">
    <a href="/test" class="bg-red-500 hover:bg-red-600 text-white text-sm px-4 py-2 rounded-md transition">Edit Profile Picture</a>
  </header>

  <!-- Main Content -->
  <main class="flex-1 p-6">
    <div class="max-w-3xl mx-auto bg-zinc-800 rounded-2xl shadow-xl p-8">
      <div class="flex justify-center mb-4">
          <img class="w-24 h-24 rounded-full object-cover border-4 border-zinc-700 shadow-lg" src="/images/uploads/<%= user.pfp %>" alt="">
      </div>
      <h3 class="text-3xl font-semibold mb-4 text-center">Hello, <%= user.username %> 🤛 {<%= user.name %>}</h3>
      <p class="text-center mb-6 text-zinc-300">What's your cerebral activity sounding like?</p>
      
      <form action="/createPost" method="post" class="space-y-4">
        <input
          type="text"
          name="caption"
          placeholder="Caption"
          class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-green-500"
        />
        <input
          type="text"
          name="body"
          placeholder="Body"
          class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-green-500"
        />
        <input
          type="submit"
          value="Post"
          class="w-full px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded-md transition"
        />
      </form>
      <div class = "posts mt-20">
        <h3 class= "text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.forEach((post)=>{%>
            <div class="post">
                <div class="post w-1/3 p-5 border-2 border-zinc-800">
                    <h4>@<%= user.username %></h4>
                    <p> <%= post.caption %></p>
                    <p> <%= post.body %></p>
                    <small class = "mt-2 inline-block "><%= post.likes.length %></small>
                    <div class ="btns flex">
                        <a class="text-blue-500" href="/like/<%= post._id %>"><%= post.likes.indexOf(user._id)? "Like" : "Unike" %></a>
                        <a class = "text-zinc-600" href="/postedit/<%= post._id %>">Edit</a>
                        <a class = "text-zinc-600" href="/postdelete/<%= post._id %>">Delete</a>
                    </div>
                </div>
              <%}) %>
            </div>
        </div>
      </div>
    </div>
  </main>

</body>
</html>
